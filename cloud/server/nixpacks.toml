[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["cd cloud/server && pip install -r requirements.txt"]

[start]
cmd = "cd cloud/server && python -m uvicorn app:app --host 0.0.0.0 --port ${PORT:-8080}"
